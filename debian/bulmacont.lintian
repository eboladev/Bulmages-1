# libbulmacont.so is no 'proper' shared library.  It's contents
# changes as needed. Upstream does not support any use beside
# for bulmacont and bulmacont-plugins (that are released to-
# gether when the API changes). Therefore we did not split out a
# libbulmacont and libbulmacont-dev pkgs. 
bulmacont: non-dev-pkg-with-shlib-symlink
bulmacont: package-name-doesnt-match-sonames

# libbulmafact.so is no 'proper' shared library.  It's contents
# changes as needed. Upstream does not support any use beside
# for bulmacont and bulmacont-plugins (that are released to-
# gether when the API changes). Therefore we did not split out a
# libbulmafact and libbulmafact-dev pkgs. 
bulmafact: non-dev-pkg-with-shlib-symlink
bulmafact: package-name-doesnt-match-sonames
